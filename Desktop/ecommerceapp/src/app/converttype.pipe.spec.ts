import { ConverttypePipe } from './converttype.pipe';

describe('ConverttypePipe', () => {
  it('create an instance', () => {
    const pipe = new ConverttypePipe();
    expect(pipe).toBeTruthy();
  });
});
